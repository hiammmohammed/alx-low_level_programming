#include <stdio.h>

/**
*main - Print numbers between 0 to 9 and letters between a to f.
*Return: Always 0 (Success)
*/
int main(void)
{
	int n, m;
	
	for (n = 48; n <= 56; n++)
	{
		if (m = 49; m <= 57; m++)
		{
			if(m > n)
			{
				putchar(n);
				putchar(m);
			}
				if (n != 56 || m != 57)
				{
					putchar('.');
					putchar(' ');
				}
		}	
	}
	putchar('\n');
	return (0);
}
